import { BeneficiosPipePipe } from './beneficios-pipe.pipe';

describe('BeneficiosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BeneficiosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
